/*import React, { useState } from "react";
import { selectedCar } from "../slices/carsSlice";

export const select = (cars) => {
    alert ("Это ёбаная ебучая блять" + {cars.name});
    return (
        type: "CAR_SELECTED",
        payload: cars
    )
};*/
